source "https://rubygems.org"

ruby "3.3.4"

gem "rails",          "~> 7.1"
gem "pg",             "~> 1.5"
gem "bootsnap",       require: false
gem "puma",           "~> 6.0"
gem "bcrypt",         "~> 3.1"
gem "jwt",            "~> 2.8"
gem "rack-cors"

# Background jobs
gem "sidekiq",        "~> 7.0"
gem "sidekiq-cron",   "~> 1.9"

# Serialization
gem "jsonapi-serializer", "~> 2.2"

# File uploads
gem "aws-sdk-s3",     require: false

# Rate limiting
gem "rack-attack"

# Admin panel
gem "administrate",   "~> 0.20"

# Redis
gem "redis",          "~> 5.0"

# SMS (swap for MSG91, Exotel etc. for India)
gem "twilio-ruby",    "~> 7.0"

group :development, :test do
  gem "dotenv-rails"
  gem "rspec-rails",  "~> 6.0"
  gem "factory_bot_rails"
  gem "faker"
end

group :development do
  gem "rubocop-rails", require: false
end

group :test do
  gem "shoulda-matchers"
  gem "database_cleaner-active_record"
end
